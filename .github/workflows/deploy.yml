name: 🚀 Deploy a cPanel vía FTP

on:
    push:
        branches: [ master, main ]     # Cubre ambas ramas por si cambias en el futuro
    workflow_dispatch:              # Permite ejecutar el workflow manualmente desde GitHub

jobs:
    build-and-deploy:
        runs-on: ubuntu-latest
        steps:
            - name: 🚚 Checkout código
              uses: actions/checkout@v4

            - name: 🛠️ Configurar Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '20'
                  cache: 'pnpm'               # Acelera instalaciones futuras

            - name: ⚙️ Instalar pnpm (acción oficial)
              uses: pnpm/action-setup@v4
              with:
                  version: latest             # Siempre la versión más reciente y estable

            - name: 📦 Instalar dependencias
              run: pnpm install --frozen-lockfile

            - name: 🔨 Build Tailwind producción
              run: pnpm tailwind:build      # Asegúrate de tener este script en package.json

            - name: 📂 Deploy vía FTP
              uses: SamKirkland/FTP-Deploy-Action@v4.3.6
              with:
                  server: ${{ secrets.FTP_SERVER }}
                  username: ${{ secrets.FTP_USERNAME }}
                  password: ${{ secrets.FTP_PASSWORD }}
                  port: ${{ secrets.FTP_PORT }}
                  protocol: ftps                # Recomendado: más seguro y común en hostings chilenos
                  local-dir: app/public/        # Solo sube lo necesario (tu carpeta de producción)
                  server-dir: /public_html/     # Ruta exacta en cPanel
                  exclude: |
                      **/.git**
                      **/.github/**
                      **/node_modules/**
                      **/src/**
                      **/.prettierrc.json**
                      **/package.json**
                      **/pnpm-lock.yaml**
                      **/tailwind.config.js**
                      # Agrega aquí más archivos/carpetas que no quieras subir (ej. .env)
